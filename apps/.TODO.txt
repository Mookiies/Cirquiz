This file is purely for notes and tracking things I want to implement going forward.

- replace when going to the home screen
- Cache and prefetch the categories. Categories shouldn't be changing while the app is running. We can just hard code these.
- Add support for trivia API and a settings page to be able to change them.
    - Along with this we should then support having multiple category selection because this api support that. also multiple difficulty

OFFLINE SUPPORT
- Store the list of categories from otdb and return that

Music

Questions:
- Support different types of questions (not just multiple choice)
    - Image questions
    - Slider questions
    - Audio questions (spotify integration like hitster)
- Generate our own questions
    - Web scraping NLP to generate a large bank that could be used offline
    - Generating a backend that is able to handle it
    - embedding for offline
- AI generated question (maybe embedding models and working with it that way)?

- MUSIC INTEGRATION

Instead of trying to be fancy and search it's probably best to use premade playlists to pull from to make things simpler.

- SPOTIFY: DOA they are deprecating the preview url not worth investing in
- DEEZER: looks promissing: https://developers.deezer.com/api/errors
- Apple Music looks promising: perhaps more widely adopted and we could let users give us a playlist URL in additon to making our own