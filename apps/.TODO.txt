This file is purely for notes and tracking things I want to implement going forward.

- When there's a fetch error it should be possible to retry the fetch instead of the only option being to quit the game on /error
- bigger tap targets

TBD enhancements
- Store the list of categories from otdb and return that
- Adding avatars in combo with the colors. i.e. Green monkey, purple demon, etc. This would be a fun way to add some personality to the game and make it more engaging.

Future Ideas:

Animations
    - Make the buttons more exciting and interactive than  just the current simple press.

Music

Questions:
- Support different types of questions (not just multiple choice)
    - Image questions
    - Slider questions
    - Audio questions (spotify integration like hitster)
- Generate our own questions
    - Web scraping NLP to generate a large bank that could be used offline
    - Generating a backend that is able to handle it
- AI generated question (maybe embedding models and working with it that way)?