This file is purely for notes and tracking things I want to implement going forward.

TBD enhancements
- Rework the header so that each screen just handles it with some shared utilities so we can have a very suble gradient game screens so it's not just straight white
- Make the handoff screen have the two additional circles shown in the mockup

OFFLINE SUPPORT
- Store the list of categories from otdb and return that

Music

Questions:
- Support different types of questions (not just multiple choice)
    - Image questions
    - Slider questions
    - Audio questions (spotify integration like hitster)
- Generate our own questions
    - Web scraping NLP to generate a large bank that could be used offline
    - Generating a backend that is able to handle it
    - embedding for offline
- AI generated question (maybe embedding models and working with it that way)?